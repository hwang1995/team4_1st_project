<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.team4.webapp.dao.ProductDescriptionsDAO">

	<select id="selectProductDescriptionsList" 
		    resultType="productdescriptions">
		select product_description_id, product_description_content, product_id
		from product_descriptions
		order by product_descriptions_id desc
	</select>
	
	<select id="selectByProductDescriptionsId"
			parameterType="long"
			resultType="productdescriptions">
		select * 
		from product_descriptions
		where 
		product_description_id = #{product_description_id}
	</select>
	
	<insert id="insertProductDescriptions"
			parameterType="productdescriptions">
		insert into product_descriptions
		(product_description_id, product_description_content, product_id)
		values
		(#{product_description_id}, #{product_description_content}, #{product_id})
	</insert>
	
	<update id="updateProductDescriptions"
			parameterType="productdescriptions">
		update product_descriptions 
		set
		product_description_content = #{product_description_content}
		where
		product_description_id = #{product_description_id}
	</update>
	
	<delete id="deleteByProductDescriptionsId"
			parameterType="long">
		delete from product_descriptions
		where
		product_description_id = #{product_description_id}
			
	</delete>

</mapper>