<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.team4.webapp.dao.ProductsDAO">
<select id="selectProductsList" resultType="products">
	select product_id, product_name, product_price, product_content, subcategory_id 
	from products
	order by product_id desc
</select>
<select id="selectByProductId" resultType="products">
	select product_id, product_name, product_price, product_content, subcategory_id  
	from products
	where product_id=#{product_id}
</select>
<insert id="insertProducts" parameterType="products">
	insert into products
	(product_id,product_name,product_price,product_content,subcategory_id)
	values
	(#{product_id}, #{product_name}, #{product_price}, #{product_content}, #{subcategory_id})
</insert>
<update id="updateProducts" parameterType="products">
	update products set
		product_name=#{product_name},
		product_price=#{product_price},
		product_content=#{product_content},
		where product_id=#{product_id}
</update>
<delete id="deleteByProductId" parameterType="long">
	delete from products where product_id=#{product_id}
</delete>
</mapper>